{
  "name": "reporivals",
  "version": "0.1.0",
  "private": true,
  "description": "RepoRivals - Competitive coding platform",
  "scripts": {
    "dev": "pnpm -r --parallel run dev",
    "dev:api": "pnpm --filter @reporivals/api dev",
    "dev:web": "pnpm --filter @reporivals/web dev",
    "dev:extension": "pnpm --filter reporivals dev",
    "build": "pnpm --filter @reporivals/shared build && pnpm -r run build",
    "build:api": "pnpm --filter @reporivals/api build",
    "build:web": "pnpm --filter @reporivals/web build",
    "build:extension": "pnpm --filter reporivals build",
    "lint": "pnpm -r run lint",
    "test": "pnpm -r run test",
    "typecheck": "pnpm -r run typecheck",
    "db:generate": "pnpm --filter @reporivals/api db:generate",
    "db:migrate": "pnpm --filter @reporivals/api db:migrate",
    "db:seed": "pnpm --filter @reporivals/api db:seed",
    "db:studio": "pnpm --filter @reporivals/api db:studio",
    "clean": "pnpm -r run clean",
    "format": "prettier --write .",
    "format:check": "prettier --check ."
  },
  "devDependencies": {
    "@types/node": "^20.10.0",
    "eslint": "^8.55.0",
    "husky": "^8.0.3",
    "lint-staged": "^15.2.0",
    "prettier": "^3.1.0",
    "typescript": "^5.3.0"
  },
  "packageManager": "pnpm@8.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=8.0.0"
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx}": [
      "prettier --write",
      "eslint --fix"
    ],
    "*.{json,md,yml,yaml}": [
      "prettier --write"
    ]
  },
  "dependencies": {
    "next-themes": "^0.4.6"
  }
}
